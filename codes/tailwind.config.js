module.exports = {
    darkMode: 'class', // Enables class-based dark mode toggling
    theme: {
      extend: {
        // You can customize the colors, fonts, etc. here if needed
      },
    },
    variants: {
      extend: {
        backgroundColor: ['dark'],
        textColor: ['dark'],
      },
    },
    plugins: [],
  }
  